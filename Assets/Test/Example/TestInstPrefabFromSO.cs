using UnityEngine;

public class TestInstPrefabFromSO : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log($"TestInstPrefabFromSO: {gameObject.name}");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
